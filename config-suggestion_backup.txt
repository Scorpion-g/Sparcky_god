/**const {
	SlashCommandBuilder,
	ChannelType,
	ChatInputCommandInteraction,
} = require('discord.js');
//const GuildConfiguration = require('../../models/GuildConfiguration');
module.exports = {
	/**
	 *
	 * @param {Object} param0
	 * @param {ChatInputCommandInteraction} param0.interaction
	 
	run: async ({ interaction }) => {
		if (!interaction.memberPermssions.has('Administrator')) {
			interaction.reply(
				"Vous n'avez pas les permissions d'executer cette commande"
			);
			return;
		}

		let guildConfiguration = await GuildConfiguration.findOne({
			guildId: interaction.guildId,
		});

		if (!guildConfiguration) {
			guildConfiguration = new GuildConfiguration({
				guilId: interaction.guildId,
			});
		}

		const subcommand = interaction.options.getSubcommand();

		if (subcommand === 'ajouter') {
			const channel = interaction.options.getChannel('salon');

			if (guildConfiguration.suggestionChannelIds.includes(channel.id)) {
				await interaction.reply(
					`${channel} est déjà un salon de suggestion.`
				);
				return;
			}

			guildConfiguration.suggestionChannelIds.push(channel.id);
			await guildConfiguration.save();

			await interaction.reply(
				`Ajout de ${channel} comme salon de suggestion.`
			);
			return;
		}

		if (subcommand === 'supprimer') {
			const channel = interaction.options.getChannel('salon');

			if (!guildConfiguration.suggestionChannelIds.includes(channel.id)) {
				await interaction.reply(
					`${channel} n'est pas un salon de suggestion.`
				);
				return;
			}

			guildConfiguration.suggestionChannelIds =
				guildConfiguration.suggestionChannelIds.filter(
					id => id !== channel.id
				);
			await guildConfiguration.save();

			await interaction.reply(
				`Suppression de ${channel} des salons de suggestion.`
			);
			retrun;
		}
	},
	options: {
		userPermission: ['Administrator'],
	},
	data: new SlashCommandBuilder()
		.setName('config-suggestion')
		.setDescription('Configurer les suggestions.')
		.setDMPermission(false)
		.addSubcommand(subcommand =>
			subcommand
				.setName('ajouter')
				.setDescription('Ajouter un  salon de suggestion.')
				.addChannelOption(option =>
					option
						.setName('salon')
						.setDescription('Le salon que vous voulez ajouter.')
						.addChannelTypes(ChannelType.GuildText)
						.setRequired(true)
				)
		)
		.addSubcommand(subcommand =>
			subcommand
				.setName('supprimer')
				.setDescription('Supprimer un  salon de suggestion.')
				.addChannelOption(option =>
					option
						.setName('salon')
						.setDescription('Le salon que vous voulez supprimer.')
				)
				.addChannelTypes(ChannelType.GuildText)
				.setRequired(true)
		),
};*/
